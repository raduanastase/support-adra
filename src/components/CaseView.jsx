// @flow
import React, {PureComponent} from 'react';

type TypeProps = {};
type TypeState = {};

export default class CaseView extends PureComponent<TypeProps, TypeState> {
  render() {
    return (
      <div>

      </div>
    );
  }
}